# Neon / Postgres
DATABASE_URL=

# Google OAuth web application
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Public deployment origin, no trailing slash
APP_URL=http://localhost:3000

# 32 random bytes encoded as base64, e.g. `openssl rand -base64 32`
APP_ENCRYPTION_KEY=
